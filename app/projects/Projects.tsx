import { Outlet } from "react-router";

export default function Projects() {
    return (
        <div>
            <h1>
                Some projects
            </h1>
            <Outlet />
        </div>
    )
}